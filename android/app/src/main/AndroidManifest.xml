<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep linking for widget -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="wallet" />
        </intent-filter>
      </activity>

      <!-- FileProvider for react-native-share -->
      <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.provider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_provider_paths" />
      </provider>

      <!-- Wallet Widgets -->

      <!-- Original Vertical Widget (Default) -->
      <receiver
        android:name=".widgets.WalletWidgetProvider"
        android:exported="true"
        android:label="Wallet - Vertical">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/wallet_widget_info" />
      </receiver>

      <!-- Horizontal Small Widget -->
      <receiver
        android:name=".widgets.WalletWidgetHorizontalSmallProvider"
        android:exported="true"
        android:label="Wallet - Horizontal Small">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/wallet_widget_horizontal_small" />
      </receiver>

      <!-- Horizontal Medium Widget -->
      <receiver
        android:name=".widgets.WalletWidgetHorizontalMediumProvider"
        android:exported="true"
        android:label="Wallet - Horizontal Medium">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/wallet_widget_horizontal_medium" />
      </receiver>

      <!-- Vertical Small Widget -->
      <receiver
        android:name=".widgets.WalletWidgetVerticalSmallProvider"
        android:exported="true"
        android:label="Wallet - Vertical Small">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/wallet_widget_vertical_small" />
      </receiver>

      <!-- Vertical Medium Widget -->
      <receiver
        android:name=".widgets.WalletWidgetVerticalMediumProvider"
        android:exported="true"
        android:label="Wallet - Vertical Medium">
        <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent-filter>
        <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/wallet_widget_vertical_medium" />
      </receiver>
    </application>
</manifest>
